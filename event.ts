document.addEventListener('DOMContentLoaded', () => {
	const form = document.getElementById('contnet') as HTMLFormElement | null;
	
	if(!form) {
		console.warn("Form element not found');
		return;
	}
});